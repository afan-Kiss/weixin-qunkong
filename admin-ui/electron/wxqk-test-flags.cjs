'use strict'

/**
 * TEST ONLY flags. Production packages MUST keep failBeforeReady=false.
 * Set true only when packaging a targeted bad-build for updater E2E.
 */
module.exports = {
  failBeforeReady: false,
}
